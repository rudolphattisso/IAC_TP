# Les valeurs sont encodées en base64 (PAS chiffrées — ne pas confondre)
# Générer : echo -n "valeur" | base64
#
# Valeurs par défaut (à changer avant déploiement) :
#   db-user             = app
#   db-password         = changeit
#   db-name             = gestion_produits
#   mysql-root-password = rootchangeit
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: gestion-produits
type: Opaque
data:
  db-user: YXBw
  db-password: Y2hhbmdlaXQ=
  db-name: Z2VzdGlvbl9wcm9kdWl0cw==
  mysql-root-password: cm9vdGNoYW5nZWl0
