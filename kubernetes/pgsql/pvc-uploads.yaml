apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-pgsql-pvc
  namespace: gestion-produits
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
